# NLANG

NLANG is the network language track.

It must prove that network-state language can become a real decision layer:

```text
network rule -> parsed predicates -> proof inputs -> action_plan -> controlled writeback
```

This directory must not contain FIELD experiments.

